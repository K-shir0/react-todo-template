import { Task } from '../domain/models/task';

type ReturnValue = {
  tasks: Task[];
};

// const useGetTasks = (): ReturnValue => {
//   const tasks = useSelector<TaskState, Task[]>(state => state.tasks);
// };
